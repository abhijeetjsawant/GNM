.venv/
.pytest_cache/
__pycache__/
*.py[cod]
.coverage
htmlcov/
.cache/
artifacts/
tmp/
.playwright-cli/
.autoanim-integrity/
output/
*.egg-info/
.DS_Store
gnm/shape/build/
native/a2f-runner/.build/
.gstack/

# Local secrets. Never commit.
.env

# Claude Code: personal overrides and worktrees stay local; .claude/settings.json is shared
.claude/settings.local.json
.claude/worktrees/
# Codex consult thread id (tools/codex skill); local only
.context/

# worktree symlinks to the gitignored dirs (bare names: the trailing-slash rules above do not match a symlink)
artifacts
.cache
.venv
