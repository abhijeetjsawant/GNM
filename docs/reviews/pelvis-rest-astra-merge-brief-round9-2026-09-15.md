# Merge review round 9 for Astra GPT6 — D7c at ladder/D7c f56d27d — 2026-09-15

Rounds 1–8 are recorded at `docs/reviews/pelvis-rest-astra-merge-review-2026-09-14.md`. The agent answered round 8 in six
commits (2f1e7e6 … f56d27d). `git diff 9dda9ac -- src/` empty. Worktree
`/Users/abhi_macbook/Projects/apps/AutoAnim/.claude/worktrees/ladder-D7c`. Read-only, adversarial, cite the line.

## What the agent reports
- `FAMILY_SWEEP`: each of the 81 exempted families names the merge conjunct whose subtree it lives in and why the card does
  not band it, from a closed vocabulary of the card's own exclusions; a family with no sweep row or with a reason the card
  does not give fails the audit as a gap (both tried). The sweep surfaced B2's bootstrap parameters and the silhouette's
  mask-cache identity, now read.
- Your three escapes closed: B1's band taken on the constituent ci95 with the producer's copies cross-checked and each
  cell's n from `cuts/<cut>/n`; identical draws enforced as a conjunct of B1 and MEASURED (`draws_used` must agree across
  parts); the σ-1 follower table reads its body constituents with the same population validator as the reread.
- Provenance by content: every build and instrument report carries the converter's sha256 and its stage; the gate hashes the
  module it resolves; the three stages kept apart (the pre-change module retained from dec1354^ so historical hygiene can be
  hashed). Six attacks NO MERGE; a path move with content unchanged still MERGE. Stated plainly: all six stamps were computed
  after the fact, so the fingerprint equality holds by construction today and becomes evidence on the next build; the log
  ordering (hygiene before the src change and every refactored stage) is derived and is ordering evidence only.
- (a) the P instrument publishes `frames_that_differ` for the contact mask and the root translation (a moved contact with
  counts unchanged, and a root moved on 7 frames, both NO MERGE). (b) 3,681 scalar leaves + 664 containers. (c) prose
  regenerated. 50 clauses, thirteen conjuncts, 36 PASS / 12 REPORT / 2 FAIL (the two stops on their bands, validated against
  their bodies). Coverage COVERED (0 gaps, sweep 81 / 16, 0 unswept, 0 stale); inventory NAMED (1,450 cross-checked, 120
  trusted, 0 unjustified); fuzz 18,360 visited, 5,130 enforced, 0 gaps, 0 monotone failures (the monotone check's direction
  was keyed to the probe constant and is now relative to the leaf's value — its second mis-specification, both recorded).
  The `draws_used` shortfall (1,838–2,000) attributed to `silhouette_partwise.py:414`'s five-frame floor, not a degenerate
  resample. Open: B2 publishes no identical-draws flag (owed); landmark byte-identity re-derivation from the .npz (debt);
  Kavan eq. 17 to D6; the fingerprint becomes evidence on the next build.

## Questions
1. MERGE at f56d27d, or not? If not, ONLY what blocks.
2. One more attack on the sweep's reasons (a family whose stated reason the card does not actually give), and one on the
   fingerprint stages.
3. Is "the fingerprint holds by construction today" an honest and sufficient disposition for THIS merge, given the close-out
   rebuild in the main checkout will produce the first genuine stamp?
4. Anything that misreads round 8, and any number you cannot reproduce.
