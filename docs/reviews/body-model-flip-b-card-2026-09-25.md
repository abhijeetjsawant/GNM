| **D4i-b the default flip, re-registered with its environment leaf named, and a close-out that enforces itself** | **Why.** D4i (`docs/reviews/body-model-flip-2026-09-25.md`) FAILED on one clause: the fitter writes its own checkout's absolute path into `/body_model/assets` in both track JSONs, so the byte-identity oracle could not hold across checkouts. Every other conjunct held. The candidate is pinned at tag `ladder/D4i-fail-b073253`, and D4i's FAIL stays on record. **This criterion is INFORMED BY D4i's result (stated, per Astra's merge round), so D4i-b demonstrates reproducibility across checkout environments and nothing about fitting accuracy or generalisation.** **Base:** `ladder/D4i-b` branches from main and merges tag `ladder/D4i-fail-b073253` as its first commit. On PASS the whole migration lands: the flip, the roster tooling, the schema-aware silhouette and verifier, the build's refusals, the bootstrap, and the coordinator's `post_merge.sh` and `ladder.py`. **Everything in D4i's card is carried VERBATIM** (the change list, the scope claim, the ownership and ORDER clause, the three-field roster by the frozen rule, must-fails i–v, the frozen population, "exit status is not a verdict", the merge rule and the FULL ROLLBACK) EXCEPT the following:
1. **THE oracle, re-stated.** For each subject, every per-subject delivered file is byte-identical to D4d's gated `--body mhr` delivery, EXCEPT the two track JSONs, where exactly ONE JSON pointer, `/body_model/assets`, must be present, be a string, and equal the building checkout's own `<toplevel>/.cache/mhr/assets`. `<toplevel>` is determined independently by `git rev-parse --show-toplevel` in the building checkout, never read from the file. The fitter resolves its SOURCE file's path and then appends `.cache/mhr/assets` without resolving it, so its spelling is verified before execution. Any disagreement (a symlinked fitter, a different spelling of the checkout, a trailing slash) is a FAIL, never normalised after the result. The WHOLE remaining JSON structure must be equal: keys, key order, types, arrays, empty containers, and added or deleted keys. It is not enough that the leaves they share are equal.
2. **Fresh evidence only.** A FRESH no-`--body` build into a clean, new output directory (cached detections may be reused if declared), a FRESH final roster run, and a fresh B1. D4i's outputs never stand in for them.
3. **B1's arm is renamed** `D4d_fitted_MHR_lod2` → `delivered_MHR`. A frozen name map is recorded, and every NUMBER must be equal under it; no numerical change may enter through a rename. The D4i extractor follows the rename.
4. **The close-out ENFORCES itself** (Astra's blocker 2 on D4i). `tools/compare/post_merge.sh closeout`:
   - requires every registered file to be PRESENT (a missing file FAILS; an empty hash is never compared);
   - compares FULL sha256s;
   - compares the track JSONs by clause 1's comparator, with main's own toplevel;
   - writes every report into a FRESH directory, refusing to reuse one;
   - compares every roster entry's own verdict field against the merge-review record, written as a committed JSON;
   - EXECUTES the registered outcome. On PASS, it leaves the MHR default in place. On anything else, it runs `tools/compare/rollback_flip.sh`, the FULL ROLLBACK scripted: `git revert` of the migration commits, the archived rig delivery restored into `artifacts/commercial-multiview-soma77`, the previous `post_merge.sh` and roster restored, and an exit that is non-zero and stated.
   FULL keeps its inherited meaning: `ladder.py` is restored as well, the pages are republished, and the step is recorded as FAIL. "Anything else" includes unreadable reports and execution failures, not only the injected mismatches. The rollback is demonstrated before the merge review in TWO places: the delivery restoration on a scratch copy of the delivery tree (checking that no MHR-only file survives), and the actual `git revert` in a DISPOSABLE checkout, recording the migration commits and how the merge parent is treated. The failure evidence is preserved in both.
5. **The gate repairs (Astra's blocker 3 on D4i):**
   - the population leg compares the registered EXPECTED against OBSERVED fields directly, never trusting `mismatches == []`;
   - roster completeness requires the 19 registered instrument IDENTITIES and their registered replacement relations, not `len(rows) >= 18`;
   - "scored rows" means per-cell rows: B1 and the silhouette PRESERVE their per-frame-camera cells as a NEW sidecar, with every existing report and number unchanged. Row equality SUPPLEMENTS summary equality and never replaces it. **D4d's REFERENCE rows are RECONSTRUCTED, because D4d never stored them** (Astra's blocker on this card):
     - the reconstruction reads D4d's hash-bound delivery and scoring inputs (the D4d GLBs and track by sha256, the frozen SAM2 masks, seed 20260921, the same draws) through UNCHANGED scoring logic with only additive row emission;
     - it must reproduce D4d's EXISTING B1 and silhouette reports exactly, or the reference is INVALID;
     - the row identity is frozen as (arm, subject, camera, frame); precision is float64 as computed, never rounded; a nonfinite cell is written as JSON null with an exclusion reason; coverage must be the full frozen population, 150 frames × 4 cameras × 2 performers per arm;
     - candidate rows are bound by sha256 to the FRESH candidate run's GLB and mesh export;
     - must-fail (v) carries through to the rows: perturbing the mesh motion must change the applicable ROWS, not just the summary;
   - the fuzz mutates only underlying evidence and never a supplied `held` flag, AND the gate itself stops trusting `held` (the pinned `d4i_flip_gate.py:248–259`) and re-derives each record-read case from its evidence;
   - the EXPECTED population comes from the frozen registration, never from another mutable report field;
   - the historical OBSERVED classes keep their original producer provenance (D4i's Phase-1 logs at the tag), and the imported migration code never stands in for the pre-migration instruments.
   Card reviewed by Astra GPT6 once, at medium effort (`docs/reviews/body-model-flip-b-astra-review-2026-09-25.md`): one blocker (reconstructing the reference rows), adopted, with the debt adopted into this text. Window 0. | 1, 7 | **Carried verbatim from D4i:** B1 reproduced exactly (now as rows under the name map), B2 PASS, closure PASS, hygiene (`--body rig` 8/8 against D7c; `src/` and `mhr_delivery.py` byte-identical; the i6 baseline's sha256 unchanged after the full roster), must-fails i–v, the frozen population, the three-field roster with (d) removed, and the coordinator's migration on the branch with the final roster run recorded. **New must-fails:**
- (vi) the oracle comparator FAILS each of these: the leaf holding another checkout's path, the leaf missing, the leaf not a string, one ADDED key, one DELETED key, one changed type, and one changed value anywhere else in the JSON;
- (vii) the close-out FAILS, and triggers the rollback on the scratch tree, for: a missing file, a one-byte change, a stale report left in a reused directory, and one entry whose verdict differs from the record;
- (viii) the repaired gate legs turn on: a changed OBSERVED population with an unchanged `mismatches` list, one DELETED roster instrument, and one changed per-cell row with unchanged summaries.

**Prediction, fixed before numbers:**
- the fresh build meets the oracle exactly as re-stated: the leaf equals this checkout's path, and every other file and every other JSON leaf is identical;
- B1's rows are equal under the name map;
- the final roster matches D4i's classes;
- every must-fail vi–viii turns;
- the rollback, demonstrated on the scratch tree, restores the rig delivery byte-identical.

**ONE verdict:** PASS iff the oracle (re-stated) ∧ B1 rows reproduced ∧ B2 ∧ closure ∧ hygiene ∧ must-fails i–viii ∧ the population ∧ the complete roster ∧ the coordinator's migration recorded ∧ the rollback demonstrated. The gate is `tools/compare/d4ib_flip_gate.py`, with a per-conjunct evidence fuzz, CRASH kept as its own class.

**Merge rule:** PASS → merge, and the close-out runs `post_merge.sh closeout` on main, which EXECUTES its registered outcome (rollback included). Non-PASS → records only on main, the branch pinned by tag. |
