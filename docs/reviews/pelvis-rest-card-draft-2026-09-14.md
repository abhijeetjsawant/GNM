# D7c card, DRAFT for review — 2026-09-14 (not yet in `LADDER_EXECUTION_PLAN.md` §2)

Written after the pre-card measurement (`tools/compare/precard/d7c_measure_oracle.py`, `d7c_take_build.py`,
`d7c_measure_take.py`; reports under `artifacts/compare/d7c-pelvis-rest/`). The plan row below is the text
that goes into §2 after D9b once the reviewer of record has seen it. Numbers are the measurement's.

---

| **D7c the pelvis on the rig's own rest** | **The defect, measured on exact truth.** `_pelvis_world_frames` (D7, mode `C_kabsch_pelvis`) fits `SOMA77_REST_PELVIS_TEMPLATE_M` — three vectors read off SOMA-77's rest (root→Spine1 39 mm, root→LeftLeg, root→RightLeg, the component-wise median of five GEM-X clips) — onto the observed {Spine1, left_hip, right_hip} about the `root` landmark. On the D3 gate's six exact-skeleton bodies the observed `root` IS the leg-root midpoint (`landmarks_from_fk`), the observed Spine1 IS the rig's `Spine` (197 mm straight up the rig's `Hips` +Y), and the template says Spine1 sits 6.87° off that axis: so the delivered pelvis reads **6.865° pitched about the hip line on every frame of every seed** (a constant, the convention's own), the `Spine` origin **21–28 mm** off truth (197 mm × sin 6.87° = 23.6), `Hips` 10 mm, the torso group **9.0–12.1 mm** on the absolute row (the 8–11 the D9b review named, hoist-subtracted here), legs 0.00; the Kabsch residual of that fit is **85–107 mm** (the template's origin and lever do not exist on this rig). **The mechanism, no constant:** the pelvis's rest frame comes from the caller's own `rest` — `_pelvis_world_frames` gains a `rest` parameter (the call site at ~:2830 passes the converter's dict; `_leg_root_offset` and `_joint_origin` already read it) — and a new source `D_rig_rest_hipline`, selected by `PELVIS_FRAME_SOURCE`: `_frame_alignment(rest[LeftUpperLeg] − rest[RightUpperLeg], rest[Spine] − mid, left_hip − right_hip, Spine1 − hip_midpoint)` with `mid = ½(rest[L] + rest[R])`. The **hip line is the exact primary axis** (two landmarks, 208–215 mm, guarded by D8c since 2026-09-06) and Spine1 − hip midpoint sets only the pitch; the `root` landmark (SOMA's `Hips`, a pelvis-interior joint whose place above the hip joints — 74–77 mm on this take — is SOMA's convention) leaves the fit entirely. Modes A/B/C and the three `SOMA77_REST_*` vectors stay in `src/` as INSTRUMENT-ONLY controls (D7's synthetic and gate call them by mode); nothing SOMA-derived is on the shipping path. **The one convention that remains is the rig's:** that SOMA's Spine1 lies on the rig's `Hips`→`Spine` axis seen from the hip midpoint. On the take that axis sits **9.4 / 9.9°** (median, performer 0 / 1) off today's delivered pelvis +Y and **3.2 / 3.9°** off the candidate's (the residual is the captured Spine1's own non-orthogonality to the hip line), i.e. this step moves the pelvis by the ~7° the pelvis-frame review priced as "what it costs if the convention is wrong" — and the exact oracle cannot see which convention a real pelvis obeys (it feeds the rig's own `Spine`); the photographs are the only referee. **The pelvis lever joins the segment-length treatment, D8c's rule on the pelvis:** frames whose |Spine1 − hip midpoint| is off the subject's own median by more than `SEGMENT_LENGTH_CEILING_FRACTION` (0.15, D8b's, NOT re-selected) are demoted to the spine's existing gap path (NaN → the per-component linear interpolation already in `_pelvis_world_frames`). Measured before the card: performer 1's lever (125 mm) is off by 16–122 % on **29 of 150 frames** (24–46, 65–81, 140–149 — the push-and-fall window and the lying end), performer 0's on none; today's 39 mm SOMA lever hid it (the hips dominate that Kabsch), a 197 mm rig lever exposes it: unguarded, the candidate's pelvis steps over 800°/s on 2 frames and the lever's p5/p95 reads −16.5 / +30.8 %; guarded, 0 frames and −14.2 / +10.6 %. **Alternative, reported not shipped (`alt`):** the same rig template through the existing unweighted Kabsch (C on `rest`, about the hip midpoint): exact on the oracle to 0.000°, but on the take it hands the unguarded Spine1 a 197 mm lever inside a three-point fit — hip residual p95 **11.9 / 31.9 mm** (max 16.6 / 58.2) against the primary-axis construction's **4.2 / 5.9** (max 7.0 / 9.7), and the leg roots swing up to 12.6 / 45.9 mm (25° of hip line on performer 1's fall) against 6.9 / 26.8 — on the honest hip line the legs must not pay for the spine. **Pre-registered on the take (converter-only, landmarks byte-identical):** pelvis pitch **−8.8 / −9.3°** median about the hip line (p95 15.4 / 23.1 guarded), the root **12.4 / 13.0 mm** median hoist-subtracted, **−12.2 / −12.9 mm fore-aft** in today's pelvis frame (80 mm × sin 9°; `_leg_root_offset` keeps the leg-root midpoint ON the captured hip midpoint to 0.0 mm, so the root moves and the legs do not), `Spine` origin 30 mm, `Neck` 12 / 10 mm (D7b's aim keeps it on its ray; the trunk-length floor 21.5 / 18.3 stands), the hoist p95 12.5 → 11.6 / 8.7 → 9.1, contacts (38, 51) → (38, 45) / (11, 18) → (6, 18): **the contact detector reads feet on leg roots the exact hip line moved by ≤ 7 / 27 mm, so contacts and the hoist MAY move and D9b's identical-contacts clause does not carry.** **What may move: everything below `Root` on every frame** (a pelvis frame is whole-take; `Root`'s identity, the finger rest curl and the eyes are the only locals expected bit-identical) — **bit-identity is not a clause here**, stated up front. **Oracle gauge:** every oracle number is on the ABSOLUTE row of `d9b_hoist_gate.py` (truth and delivery share one world); the D3 gate's `retarget_cost.score` is leg-root-aligned, reads arms 2.72 since D9b, its band is a standing fail and is not moved. **D7's own instruments will read this WORSE by ~7° by construction** — their truth is SOMASKEL77 posed, whose pelvis IS the SOMA convention (`d7_pelvis_synthetic.py`, `d7_pelvis_frame_gate.py` B1/B2); pre-registered as expected and attributed, not a band; the gate's `convention_residual_with_the_SHIPPED_constants` clause goes moved-by-design and is handed to the instrument-debt re-pin with the D3 gate's frozen references. Window 0; `PELVIS_SMOOTHING_FRAMES`, `PELVIS_MINIMUM_RESOLVED_FRACTION` and the 0.15 ceiling are not re-selected. No MAMMA-referenced selection. | 7 | **hygiene, before any src change:** today's code on the same inputs byte-identical to the shipped delivery (8 of 8; the pre-card's `--mode shipped` rebuild already reads 8 / 8) · **instrument first, `tools/compare/d7c_pelvis_rest_gate.py`, on the SHIPPED build and the six D3 bodies:** reproduce the pre-card — 6.865° on every seed, Spine 21–28 mm, torso 9.0–12.1 absolute, Kabsch residual 85–107 mm; on the take the delivered pelvis +Y vs Spine1 − hip midpoint 9.4 / 9.9°, the lever's 29 off frames on performer 1 · **REFACTOR TRIPWIRE, a must-pass before delivery:** with `PELVIS_FRAME_SOURCE` held at `C_kabsch_pelvis` the refactored `_pelvis_world_frames(points, spine, rest=...)` reproduces D9b bit-for-bit, 8 of 8 on the take and every rotation / root / contact on the six bodies (the seven-normalisations lesson) · **O1 (the oracle, absolute row, every seed):** pelvis vs truth **≤ 0.01° on every frame** (from 6.865); `Spine` and `Hips` origin miss hoist-subtracted **≤ 0.01 mm** (from 21–28 / 10); torso **0.00** on the unhoisted frames and hoist-sized on the hoisted ones (that is the plant, D9b's 13.3, not the pelvis); legs **0.00** unchanged; the Kabsch/frame residual ≤ 1e-6 m (not a rank-2 degenerate: the rig template is coplanar, the det-sign branch is exercised — assert the residual, not the tilt) · **O2:** legs, feet, toes, contacts bit-identical per seed, the hoist within 0.01 mm (the hip line is exact on truth, so the leg roots do not move) · **O3 (reported, the D3 gate's own aligned gauge):** arms 1.32–2.72 → the pre-card reads **0.07–0.60** (the trunk is exact, so what remains is D2's clavicle construction); the D3 arm band (0.5) then passes 5 of 6 seeds and stays a standing FAIL on the sixth (0.60) — reported, not re-pinned here · **S (SELECTOR, synthetic truth, the new instrument):** the six D3 bodies under I7/I8's heavy-tail frame-correlated pixel noise through `triangulate_point` on the fixture's own cameras (`d7_pelvis_synthetic.py`'s path, its truth replaced by the D3 bodies — the SOMA-posed truth cannot referee the constant it carries), five arms on one denominator: `D_rig_rest_hipline` (candidate), C-on-rest (alt), C-on-SOMA (today), a world-vertical pelvis, thorax-as-pelvis; **rule, fixed before numbers:** the candidate's pelvis error vs truth on the bent tercile is below today's and within 1° of the alt's, AND its leg-root error is below the alt's on every seed (by construction; a violation stops the step); the truth's own tilt range is reported first and if the world-vertical control lands within 2° of it the control is declared non-discriminating (D7's lesson) rather than a pass; lever breaks injected at the take's measured +30 % on 20 % of frames must fire the guard and must not move the hip line · **B1 the photographs (the band the candidate cannot optimise):** `d7b_silhouette_partwise` D9b vs candidate on identical draws, TORSO+LEGS and ARMS, whole take and the bent tercile, both performers, **not worse, CI upper bound ≥ 0 on the D7b/D8 predicate**; TORSO+LEGS on performer 1's bent tercile is where a 9° pelvis and a 13 mm root would show if the rig's convention is the wrong one — improvement is NOT predicted; the MAMMA mesh oracle bit-identical · **B2 `delivered_vs_capture.py` D9b vs candidate, `--reference smoothed`, identical draws:** landmarks byte-identical so the same-denominator clause is an expected PASS (CHANGED means the change did more than refit the pelvis); leg-root midpoint on the captured hip midpoint 0.0 mm; hips within the hip-line exactness (the per-hip miss ≤ the rig half-span's spread, p95 4 / 6 mm); knees, ankles, neck REPORTED with their floors (the neck at 21.5 / 18.3, D7b's trunk-length floor; the legs' 4.6–9.2 mm ray miss is D9-legs') · **B3 the hoist and contacts, before and after, reported:** per-frame hoist (p95 12.5 → ~11.6 / 8.7 → ~9.1), contact counts and runs, `penetration_before`, the lowest foot; every root figure read with each frame's hoist subtracted · **B4 the pelvis itself, reported never banded:** pitch change per frame; the delivered +Y vs Spine1 − hip midpoint (9.4 / 9.9 → ~3.2 / 3.9°); the frame's step per frame (p95 6.4 → ~12.3 / 9.9 → ~12.1; the 131 mm lever now carries Spine1's jitter that the 39 mm one averaged into the hips) and frames over 800°/s (0 → 0 guarded; 2 unguarded on performer 1 — the guard's own must-fail); the captured hip half-span in the delivered pelvis frame vs the rig's own: p5 / p95 **−3.4 / +4.0 %** and **−3.9 / +4.8 %** on D8c's honest mask, inside D8b's 15 % ceiling — **D8c's both-hips convention stands** (the spread D8c measured against SOMA's `root` no longer exists; this is its rig-frame reading, symmetric by construction because the hip line is the primary axis) · **B5 the head gate** rerun (reads the head solve from disk; `Head`'s rotation is the absolute solve, its POSITION moves with the trunk — reported) · **must-fails:** the D9b build itself (6.865° on every oracle frame; fails O1); a pelvis frozen upright (D7's control, the photographs disqualified it at −0.218 IoU; on the oracle it fails O1 by the truth's own tilt); a fit that reproduces D7's constants (the SOMA template fed through the new path: 6.865°, fails O1 — and it is the tripwire's PASS, the two are the same run read against different references); the alt (C on rest) — exact on O1 and disqualified on the take by the hip residual and the leg-root swing above; the unguarded candidate (2 frames over 800°/s on performer 1, lever p95 +30.8 %); **a known blindness stated:** a template built from `rest` about `Hips` instead of the hip midpoint reads 0.000° on the oracle too (the rig's rest is symmetric about +Y, so the 80 mm origin error is absorbed) and is exposed only by its 84 mm Kabsch residual — the tilt does not discriminate the origin, the residual does · **merge rule, fixed before numbers:** hygiene AND the tripwire AND O1 AND O2 AND S AND B1 on both performers AND B2's same-denominator PASS; O3, B3, B4, B5 report · **handoffs:** the remaining convention (Spine1 on the rig's pelvis axis) to lane H's marker session; D7's instruments' moved-by-design clauses and the D3 gate's gauge to instrument debt; the pelvis lever's 29 broken frames to the detector (D8d, if the guard's demote is not enough on the photographs); the trunk's length residual under flexion to D5 |

---

## What the pre-card measured, in one table

| quantity | shipped (D9b) | candidate (b) | alt (a, Kabsch on rest) |
|---|---|---|---|
| oracle: pelvis vs truth, every seed, every frame | 6.865° | 0.000° | 0.000° |
| oracle: `Spine` origin miss, hoist-subtracted, median over seeds | 21.0–28.3 mm | 0.00 | 0.00 |
| oracle: torso absolute, hoist-subtracted | 9.0–12.1 mm | 0.00 | 0.00 |
| oracle: arms, the D3 gate's aligned gauge | 1.32–2.72 | 0.07–0.60 | 0.07–0.60 |
| oracle: Kabsch residual of the fit | 85–107 mm | 0.000 | 0.000 |
| take: hip residual p95 / max (perf 0 · perf 1) | 22 / 26 · 37 / 66 (about SOMA's root) | 4.2 / 7.0 · 5.9 / 9.7 | 11.9 / 16.6 · 31.9 / 58.2 |
| take: leg joints move vs shipped, p95 / max | — | 5.1 / 6.9 · 12.0 / 26.8 | 7.1 / 12.6 · 23.1 / 45.9 |
| take: pelvis pitch change, median | — | −8.8 / −9.3° | −8.8 / −9.3° |
| take: root move hoist-subtracted, median (fore-aft) | — | 12.4 / 13.0 (−12.2 / −12.9) | 12.4 / 13.1 |
| take: delivered +Y vs Spine1 − hip mid, median | 9.4 / 9.9° | 3.2 / 3.9° | 1.4 / 2.0° |
| take: pelvis step per frame p95, frames > 800°/s | 6.4 / 9.9, 0 / 0 | 12.3 / 16.6, 0 / 2 (guarded: 12.1, 0) | — |
| take: contacts | (38, 51) / (11, 18) | (38, 45) / (6, 18) | (36, 36) / (4, 18) |
| take: hygiene (shipped mode rebuild vs shipped bytes) | 8 / 8 identical | — | — |
