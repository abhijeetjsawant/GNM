# Review brief for Astra GPT6 — the D4 card, the body model in the delivery path — 2026-09-21. ONE ROUND.

You are the reviewer of record for the AutoAnim body-capture lane. Since 2026-09-15 the rule is one card review and one
merge review per step; findings about a gate instrument that do not reach a card-banded verdict are instrument debt.
Answer adversarially, cite the deciding line, and list ONLY what would block dispatch, then the rest as debt.

## State
D7c merged 2026-09-15. The user asked whether the lane is worth continuing (delivered joints ~47 mm from MAMMA's, silhouette
0.65 against MAMMA's mesh at 0.87). The coordinator measured the body-model route before any card: read
`docs/reviews/body-model-precard-2026-09-21.md` (the pre-registration, the table, the paired CIs, the caveats) and the
scripts `tools/fitter/fit_delivery_mhr.py`, `tools/compare/d4_silhouette_paired.py`, `tools/compare/blender_export_mesh_momentum.py`;
reports under `artifacts/compare/d4-body/`. The user chose shape (a): MHR's own mesh is the delivered body. The MAMMA
oracle rerun of `silhouette.py` on the fitted arm is running now (SMPL-X was restored from the Modal volume today).

## The card, verbatim (docs/LADDER_EXECUTION_PLAN.md §2, after D7c)
| **D4 the body model in the delivery path** | The picture after D7c is the MESH: a stock MPFB asset stretched over the scaled rig with its old skin weights (silhouette 0.647 / 0.652 against MAMMA's mesh at 0.87 / 0.84). **Measured before the card (2026-09-21, `docs/reviews/body-model-precard-2026-09-21.md`):** MHR (Meta's open body model, Apache; momentum, MIT; from MHR's own release, 68 raw scale channels, never the SAM-licensed PCA) fitted by momentum's two-stage calibration to the delivered take's own raw triangulated landmarks with locator offsets PINNED (limit_weight 10, FITTER_PLAN §7's honest setting) and its own mesh delivered as a skinned GLB reads **0.789 / 0.730** pooled IoU on the committed silhouette instrument, paired per frame against the D7c delivery: **+0.142 [0.120, 0.150] / +0.078 [0.053, 0.093]**; MHR's MEAN body driven by the same tracker reads 0.744 / 0.722, so the body model is most of the gain and the fit adds +0.045 (CI clear) / +0.007 (CI through zero). **Mechanism (the user's decision, 2026-09-21: shape (a), MHR's own mesh IS the delivered body):** `scripts/build_commercial_multiview_comparison.py` gains `--body mhr` (default stays `rig` until this step merges, then flips): after the converter's smoothed landmarks (the SAME denominator as the rig, with D8/D8b/D8c's repairs — the pre-card used the raw array, a stated confound), momentum fits identity (stage A/B, pinned offsets) and per-frame pose per performer, at MHR **lod2** (10,661 vertices; lod6's 595 was the pre-card's handicap), and writes `subject-XX.glb` (skinned, 30 fps, the marker spheres stripped) plus a body-track JSON/npz carrying MHR's joint names, the 68 identity channels and the 136 pose channels per frame under a NEW schema version (`autoanim.body-track/2.0-mhr`); the rig path is untouched and remains selectable. No constant is fitted; the momentum configs (`calib_frames` 100, `loss_alpha` 2.0, `max_iter` 30, smoothing 0) are the pre-card's and are recorded as engineering settings, not selected. MAMMA enters nowhere. **What may move: everything** — a different body, mesh and skeleton — so no bit-identity clause; the rig delivery is archived as the before arm. Window 0. Card reviewed by Astra GPT6 once (one round, by the 2026-09-15 rule) | 6 | **hygiene:** `--body rig` rebuilds the D7c delivery 8 of 8 byte-identical · **the pre-card reproduced through the build script:** `--body mhr` at lod6 on the RAW array reproduces the pre-card's 0.789 / 0.730 to 0.001 (the instrument path is the pre-card's) · **O1 exactness on synthetic truth (the oracle):** six MHR bodies with known identity channels drawn in [0.7, 1.3] on the 16 D3 channels' MHR counterparts, posed with the D3 gate's donor motion mapped onto MHR, landmarks from their locators: the fit recovers the joint positions to ≤ 1 mm and the delivered GLB's FK reproduces the track to ≤ 1e-4 m (D3's closure band); the mean body on the same landmarks MUST miss by the injected scale (the must-fail) · **B1 the photographs, THE band:** part-wise and whole silhouette against the SAM2 masks, whole take and the bent tercile, both performers, identical draws, block bootstrap: the delivered MHR body beats the D7c delivery with the CI clear of zero on both performers (predicted from the pre-card: ≥ +0.10 / +0.05 pooled); the MHR mean body reported beside it (the fit's own share, no band); MAMMA's mesh through the same rasteriser bit-identical to its committed value (the instrument unchanged); the instrument's frozen-pose and mean-body controls must stay below · **B2 same denominator:** the smoothed landmark arrays byte-identical to the D7c build's on both performers · **B3 placement, reported:** every MHR locator joint against our own captured landmarks (the delivered-vs-capture instrument's role, on the MHR joint set), hips / knees / ankles / neck medians, and the fitted segment lengths against the performer's measured ones (FITTER_PLAN §7's table, expected ~20 mm mean abs error, the pinned-offset under-fit stated) · **B4 the MAMMA arm, reported never selected:** MHR joints vs `pred_joints` through the subject map, bias and spread separately · **B5 delivered bytes:** the GLB's sampler times, 150 frames at 30 fps, rest/hierarchy/skin weights against MHR's, the mesh's per-frame vertex count and finite-ness, the head/eye locators' facing dot > 0 · **must-fails:** the D7c rig delivery itself (fails B1); the MHR mean body (fails O1 by the injected scale); a fit with free locator offsets (offsets median > 50 mm and the identity channels at zero: the mean-body regressor in disguise, FITTER_PLAN §7) · **merge rule, fixed before numbers:** hygiene AND the reproduction AND O1 AND B1 on both performers AND B2; B3, B4, B5 report · **handoffs:** the pelvis/hip conventions and the offset identifiability to lane H's markers; the finer LODs and the compositor's consumption of the MHR body (`unified_gltf`, the N5.1 assembly, which today consume the MPFB body) to the integration step after this one; D6 is absorbed (there is no MPFB mesh to re-skin on this path) and D5's scaling becomes the marker-session step |

## Questions
1. Dispatchable? If not, ONLY what blocks, in order.
2. O1: is "six MHR bodies with known identity channels, posed with the D3 donor motion mapped onto MHR, landmarks from
   their locators" a sound exactness oracle for a momentum fit with PINNED offsets — or does pinning make the oracle
   trivially exact (locators at the joints) while the take's landmarks are not at the joints? What must-fail catches a
   fit that only ever recovers the mean?
3. B1 is the band and the candidate cannot optimise it directly; but a fit that inflates the body would raise recall and
   IoU. Is IoU alone enough, or must precision and recall be reported per part with a stated rule?
4. The denominator: the rig used the smoothed, repaired landmarks; the card says the MHR fit consumes the same array. Is
   there any sense in which the MHR arm and the rig arm still differ in input (the toes, the spine, the head solve the
   rig consumes and momentum does not)? State what B2 must compare.
5. Anything in the card a constant can pass, and any number you cannot reproduce from the pre-card artifacts.
